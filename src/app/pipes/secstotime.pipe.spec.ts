import { SecstotimePipe } from './secstotime.pipe';

describe('SecstotimePipe', () => {
  it('create an instance', () => {
    const pipe = new SecstotimePipe();
    expect(pipe).toBeTruthy();
  });
});
